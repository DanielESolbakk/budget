name: "Planning issue lint (deprecated)"

on:
  workflow_dispatch:

permissions:
  issues: write

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is deprecated
        run: echo "Planning issue lint has been consolidated into Planning Issue Validation workflow. No action taken."
