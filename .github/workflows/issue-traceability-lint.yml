name: Issue Traceability Lint (deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  deprecated-notice:
    name: Deprecated
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is deprecated
        run: echo "Planning issue traceability lint has been consolidated into Planning Issue Validation workflow. No action taken."
